#include "State.h"
#include "AI.h"

State *AI::generateState(void *info)
{
     State *ret = new State();
     
     // Convert the info coming from the UE4 simulation into
     // a state that can be used by the AI

     return ret;
}
